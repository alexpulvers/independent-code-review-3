import { reportAge } from "./../src/main.js";

describe("reportAge()", () => {
  test("should return a value for each property in reportAge()", () => {
    let age = 28;
    reportAge(age);
  });
});
